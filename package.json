{
  "name": "grafana-foundation-testing-next",
  "version": "1.0.0",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "build": "mkdir -p resources && node index.ts > resources/output.json",
    "build-prod": "mkdir -p cloud-dashboards && node index.ts > cloud-dashboards/output.json",
    "format": "biome format --write *.ts *.md *.jsonc .github/* cloud-dashboards/*.json resources/*.json; prettier --write *.md .github/*.md",
    "lint": "biome lint --fix *.ts *.md *.jsonc .github/* cloud-dashboards/*.json resources/*.json",
    "assist": "biome check --fix *.ts *.md .github/* cloud-dashboards/*.json resources/*.json",
    "validate": "grafanactl resources validate",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "@grafana/grafana-foundation-sdk": "0.0.8"
  },
  "devDependencies": {
    "prettier": "3.8.1",
    "@biomejs/biome": "2.4.2"
  }
}
